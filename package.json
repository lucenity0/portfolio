{
  "name": "lucenity-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Terminal/desktop-OS style portfolio for me.lucenity.dev",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
